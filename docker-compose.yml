---
services:
  build_app:
    build: python3_image
    volumes:
      - /home/server1/settings:/usr/src/app/settings
  pushgateway:
    image: prom/pushgateway
    ports:
      - "9091:9091"
version: "3.8"
